from django.urls import re_path
from . import consumers

# Events WebSocket URL patterns
websocket_urlpatterns = [
    # Add event-specific WebSocket patterns here if needed
    # For now, empty list
]
